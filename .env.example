SESSION_SECRET=change-this-to-a-long-random-string
PORT=3000
DB_PATH=./data.db

# Optional: SMTP for email notifications. If omitted, emails log to console.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com

# Your email address to receive admin notifications (flags, etc.)
ADMIN_EMAIL=
